import React, { Fragment } from 'react';

const Spinner = () => (
  <Fragment>
    <div
     
      style={{ width: '200px', margin: 'auto', display: 'block' }}
      >
        Loading...
      </div>
  </Fragment>
);

export default Spinner;